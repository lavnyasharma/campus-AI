import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Campus AI Program - CoRover.ai | Experience the Future. Build with AI.",
  description:
    "Transform your career with comprehensive AI training, hands-on projects, and industry certification. Join the AI revolution with CoRover's Campus AI Program.",
  keywords: "AI training, machine learning, artificial intelligence, campus program, certification, CoRover",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem={false} disableTransitionOnChange={false}>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
